# pylint: disable=C0114

def fib_r(limit: int) -> int:
    """
    Calculates the Fibonacci sersies up to `limit`'s element.
    If n = limit, then the last series element is:
    Fn = Fn-1 + Fn-2, like for limit = 6:
    F0 : 0
    F1 : 1
    F2 : 0 + 1 = 1
    F3 : 1 + 1 = 2
    F4 : 2 + 1 = 3
    F5 : 3 + 2 = 5
    F6 : 5 + 3 = 8

    Function must use recurrency.

    Parameters
    ----------
    limit: int
        The last series element.

    Returns
    -------
    int
        The series limit's element value.
    """


def fib_l(limit: int) -> int:
    """
    Calculates the Fibonacci sersies up to `limit`'s element.
    If n = limit, then the last series element is:
    Fn = Fn-1 + Fn-2, like for limit = 6:
    F0 : 0
    F1 : 1
    F2 : 0 + 1 = 1
    F3 : 1 + 1 = 2
    F4 : 2 + 1 = 3
    F5 : 3 + 2 = 5
    F6 : 5 + 3 = 8

    Function must use single loop.

    Parameters
    ----------
    limit: int
        The last series element.

    Returns
    -------
    int
        The series limit's element value.
    """
